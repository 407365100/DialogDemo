<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/color_00000000"
    android:padding="@dimen/dp_20"
    android:orientation="vertical">

    <ListView
        android:id="@+id/lv_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="@color/color_00000000"
        android:divider="@color/color_dedede"
        android:dividerHeight="@dimen/dp_0d5"
        android:fadingEdge="none"
        android:listSelector="@color/color_00000000"
        android:background="@drawable/dialog_ios_bg_selector"
        android:scrollbars="none" />
    <TextView
        android:id="@+id/tv_cancel"
        android:layout_marginTop="@dimen/dp_20"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_50"
        android:layout_centerVertical="true"
        android:ellipsize="end"
        android:gravity="center"
        android:text="取消"
        android:singleLine="true"
        android:textColor="@color/color_666666"
        android:background="@drawable/dialog_ios_bg_selector"
        android:textSize="@dimen/sp_16" />
</LinearLayout>